{
  "name": "developer-token",
  "version": "1.0.0",
  "description": "Generate developer tokens for Apple using the JWT standard.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dylan Wishner",
  "license": "ISC",
  "dependencies": {
    "browserify": "^16.5.0",
    "jsonwebtoken": "^8.5.1"
  }
}
